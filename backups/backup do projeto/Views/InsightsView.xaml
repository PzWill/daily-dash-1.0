<Window x:Class="DailyDash.Views.InsightsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DailyDash.Views"
        mc:Ignorable="d"
        Title="AnÃ¡lise e Insights" Height="600" Width="800"
        Background="{DynamicResource AppBackgroundBrush}"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="40" GlassFrameThickness="0" CornerRadius="12"/>
    </WindowChrome.WindowChrome>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Background="{DynamicResource PanelBackgroundBrush}" Padding="20,15" BorderBrush="#20FFFFFF" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="ðŸ“ˆ AnÃ¡lise e Insights" Foreground="White" FontSize="{DynamicResource FontSize_20}" FontWeight="SemiBold" VerticalAlignment="Center" />
                
                <StackPanel Grid.Column="1" Orientation="Horizontal" WindowChrome.IsHitTestVisibleInChrome="True">
                    <Button Content="âœ–" Width="30" Height="30" Background="Transparent" Foreground="White" BorderThickness="0" FontSize="{DynamicResource FontSize_16}" Cursor="Hand" Click="CloseButton_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="{DynamicResource PanelBackgroundBrush}" Margin="20" CornerRadius="8" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- Left Column: Metrics -->
                <StackPanel Grid.Column="0">
                    <TextBlock Text="MÃ©tricas Gerais (HistÃ³rico)" FontSize="{DynamicResource FontSize_16}" FontWeight="SemiBold" Foreground="#FFD700" Margin="0,0,0,20"/>
                    
                    <Border Background="#10FFFFFF" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                        <StackPanel>
                            <TextBlock Text="Total de Tarefas DiÃ¡rias ConcluÃ­das" Foreground="#AAA" FontSize="{DynamicResource FontSize_12}"/>
                            <TextBlock Text="{Binding TotalTasksCompleted}" Foreground="White" FontSize="{DynamicResource FontSize_28}" FontWeight="Bold" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>
                    
                    <Border Background="#10FFFFFF" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                        <StackPanel>
                            <TextBlock Text="MÃ©dia de Tarefas DiÃ¡rias" Foreground="#AAA" FontSize="{DynamicResource FontSize_12}"/>
                            <TextBlock Text="{Binding AverageDailyTasks}" Foreground="White" FontSize="{DynamicResource FontSize_28}" FontWeight="Bold" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
                
                <!-- Right Column: Tag Distribution -->
                <StackPanel Grid.Column="2">
                    <TextBlock Text="DistribuiÃ§Ã£o por Tag (DiÃ¡rias)" FontSize="{DynamicResource FontSize_16}" FontWeight="SemiBold" Foreground="#FFD700" Margin="0,0,0,20"/>
                    
                    <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="350">
                        <ItemsControl ItemsSource="{Binding TagDistribution}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#10FFFFFF" CornerRadius="8" Padding="12" Margin="0,0,0,10">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="15"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <Ellipse Width="10" Height="10" Fill="{Binding ColorHex}" VerticalAlignment="Center"/>
                                            <TextBlock Grid.Column="1" Text="{Binding TagName}" Foreground="White" Margin="10,0,0,0" VerticalAlignment="Center"/>
                                            <TextBlock Grid.Column="2" Text="{Binding Count}" Foreground="#AAA" FontWeight="Bold" VerticalAlignment="Center"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>

